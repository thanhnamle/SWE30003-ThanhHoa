{
  "name": "smartfm-monorepo",
  "version": "1.0.0",
  "scripts": {
    "start": "npx --yes concurrently \"npm run dev --prefix frontend\" \"dotnet run --project backend/src/SmartFM.API\"",
    "install:all": "npm install && npm install --prefix frontend",
    "db:update": "dotnet ef database update --project backend/src/SmartFM.Infrastructure --startup-project backend/src/SmartFM.API"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
